{
  "name": "covid-patient-timeline",
  "version": "0.0.1",
  "private": true,
  "workspaces": {
    "packages": [
      "modules/*"
    ],
    "nohoist": [
      "/modules/frontend/**"
    ]
  },
  "scripts": {
    "backend": "yarn --cwd=./modules/backend",
    "frontend": "yarn --cwd=./modules/frontend",
    "db": "yarn --cwd=./modules/db"
  }
}
